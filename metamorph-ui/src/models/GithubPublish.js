export class RepositoryData {
    constructor(userName, accessToken, repositoryName) {
        this.userName = userName;
        this.accessToken = accessToken;
        this.repositoryName = repositoryName;
    }
}